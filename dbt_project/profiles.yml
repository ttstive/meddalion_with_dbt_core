dbt_project:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: dbt-project-free
      dataset: "{{ env_var('DBT_DBNAME', 'dbt_dw') }}"
      threads: 4
      keyfile: protected/key_dbt_gcp.json
      location: US